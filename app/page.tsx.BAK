import HomeBlob from "@/components/homeBlob";
import Image from "next/image";
import { useId } from "react";

export default function Home() {
  const titleId = useId();
  return (
    <>
      <section className="relative mt-16 flex items-center justify-center text-center md:h-[80vh]">
        <div className="absolute left-28 hidden aspect-[365/478]  -rotate-6 bg-[conic-gradient(from_163deg_at_46.03%_87.39%,var(--tw-gradient-stops))] from-gray-500 to-[#FFF_212deg] md:block md:w-80 ">
          <Image
            src="/clement.png"
            alt="bg"
            layout="fill"
            objectFit="contains"
            quality={100}
          />
        </div>
        <h2 className="relative w-10/12 text-7xl font-black uppercase md:w-2/4">
          Bonjour, je suis Clément lascar Ux/ui designer
        </h2>
        <h2
          id={titleId}
          className="absolute w-10/12 translate-y-8 bg-black pb-16 text-7xl font-black uppercase  text-white [clip-path:url(#clip0)] md:w-2/4 "
        >
          Bonjour, je suis Clément lascar Ux/ui designer
        </h2>
        <span className="absolute bottom-24 right-36 rotate-[14deg] font-black uppercase">
          Scroll
        </span>
        <HomeBlob id="clip0" targetId={titleId} />
      </section>
    </>
  );
}
